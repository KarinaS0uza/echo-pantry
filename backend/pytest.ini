[pytest]
DJANGO_SETTINGS_MODULE = tests.settings
django_find_project = false
pythonpath = .
testpaths = tests
python_files = test_*.py
addopts = --strict-config --strict-markers -ra
markers =
    sqlite_file: requires --sqlite-file and transactional_db for independent connection checks
